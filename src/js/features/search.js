export const filteredTasks=(tasks,{query,status,deadline,tag},sort)=>{const now=new Date(),end=new Date(now);end.setDate(now.getDate()+7);let a=tasks.filter(t=>(!query||`${t.title} ${t.description} ${(t.tags||[]).join(' ')}`.toLowerCase().includes(query.toLowerCase()))&&(status==='all'||t.status===status)&&(tag==='all'||(t.tags||[]).includes(tag)));if(deadline!=='all')a=a.filter(t=>{if(!t.deadline)return false;const d=new Date(t.deadline);return deadline==='overdue'?d<now&&t.status!=='done':deadline==='today'?d.toDateString()===now.toDateString():d<=end&&d>=now});return a.sort((x,y)=>sort==='priority'?({high:0,medium:1,low:2}[x.priority]-{high:0,medium:1,low:2}[y.priority]):sort==='created'?y.createdAt.localeCompare(x.createdAt):(x.deadline||'9999').localeCompare(y.deadline||'9999'))};
export const todayTasks=tasks=>{const now=new Date();return tasks.filter(t=>t.deadline&&new Date(t.deadline).toDateString()===now.toDateString())};
