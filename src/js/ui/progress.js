import {isDone} from '../models/task.js';export const renderProgress=(tasks)=>{const done=tasks.filter(isDone).length,p=tasks.length?Math.round(done/tasks.length*100):0;document.querySelector('#progress-ring').style.setProperty('--progress',p+'%');document.querySelector('#progress-value').textContent=p+'%';document.querySelector('#progress-count').textContent=`${done} of ${tasks.length} complete`};
