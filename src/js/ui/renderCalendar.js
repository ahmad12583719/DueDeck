import {monthDays} from '../features/calendar.js';const esc=s=>String(s).replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));export const renderCalendar=(root,tasks)=>{const d=new Date(),{offset,days}=monthDays(d.getFullYear(),d.getMonth()),names=['Sun','Mon','Tue','Wed','Thu','Fri','Sat'];let cells=names.map(x=>`<div>${x}</div>`).join(''),body='';for(let i=0;i<offset+days;i++){const n=i-offset+1;if(n<1){body+='<div class="cal-cell muted"></div>';continue}const date=new Date(d.getFullYear(),d.getMonth(),n),today=date.toDateString()===d.toDateString(),ts=tasks.filter(t=>t.deadline&&new Date(t.deadline).toDateString()===date.toDateString());body+=`<div class="cal-cell ${today?'today':''}"><div class="cal-date">${n}</div>${ts.map(t=>`<div class="cal-task" style="background:${t.color}" title="${esc(t.title)}">${esc(t.title)}</div>`).join('')}</div>`}root.innerHTML=`<div class="calendar"><div class="cal-head">${cells}</div><div class="cal-grid">${body}</div></div>`};
